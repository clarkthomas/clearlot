# Architecture

WebMCP page + MCP /mcp → Clearlot API → intent DB + matcher + x402 fee gate.
Matcher: Global Catalog then allowlisted storefronts. Rank in-stock, under max, has checkout URL, ships to country.
Never collect cards. Never unofficial place_order scrapers. No operator escrow wallet.
